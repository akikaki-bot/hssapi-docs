## GET /school/\{id\}/\{grade\}/\{class\}

:::warning
認証が必須です。
:::

クラスのデータを取得します。

### リクエスト

```http
GET /school/:id/:grade/:class
```

### レスポンス ( Example )

[ userDatas ](../../../types/userDatas.mdx)を返します。