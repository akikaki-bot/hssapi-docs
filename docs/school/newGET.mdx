# GET /v1/school/:id/userdatas/:grade/:class/ 

クラスデータを返却します。

### Request

```http 
GET /v1/school/:id/userdatas/:grade/:class
```

### Response

[UserDatas](../types/userDatas) desu

### Error Responses

- HTTP 404
  - クラスデータが存在しません。